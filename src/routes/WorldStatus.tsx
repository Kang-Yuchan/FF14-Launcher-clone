import React, { FC } from 'react';
import Navigation from '../components/Navigation';

const WorldStatus: FC = () => {
  return (
    <div>
      <Navigation />
      World Status
    </div>
  );
};

export default WorldStatus;
