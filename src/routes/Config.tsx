import React, { FC } from 'react';
import Navigation from '../components/Navigation';

const Config: FC = () => {
  return (
    <div>
      <Navigation />
      Config
    </div>
  );
};

export default Config;
